import chai from 'chai';

import { BadRequest } from '../../server/helpers/errors';
import weather from '../../server/routes/weather';

describe('[Model] Weather', function () {
    xit ('should be able to create a new weather forecast', function () {
        // TODO: use mockery to test weather model
    });
});